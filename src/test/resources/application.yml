spring:
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: create # 배포 환경에선 validation으로 하기! 꼭!
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    username: root
    password: 100101
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/student_tool?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test
            client-secret: test
            scope: email, profile
  session:
    jdbc:
      initialize-schema: always
google:
  slides:
    credentials-file-path: test
    tokens-directory-path: test
    credentials-forupdate-file-path: test
    folder-path: test
aws:
  s3:
    accessKey: test
    secretKey: test
    bucketName: testBucket
